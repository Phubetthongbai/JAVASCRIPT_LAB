const Listname = ['Just','sara',[christ]];


for (let index = 0; index < Listname.length; index++) {
    const element = Listname[index];
    console.log(element);
}

while (index < Listname.length){
    const list = Listname[list];
    console.log(list)
}

for (let list of Listname){
    console.log(list);
}